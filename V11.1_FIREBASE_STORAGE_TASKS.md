📋 V11.1_FIREBASE_STORAGE_TASKS.md
Versão: v11.1 (Migration Base64 → Firebase Storage)
Branch Git: V3.2-firebase-storage
Objetivo: Infraestrutura genérica para assets pesados (imagens, PDFs, DXF)
Destinatário: Agente IDE (Claude Code, Cursor, etc.)

📖 INSTRUÇÕES PARA O AGENTE IDE
Antes de começar, LEIA pela ordem:

MASTER.md — Boot sequence + referência rápida
RISCOS.md — IDs protegidos, funções críticas, dependências
ESPECIFICACAO.md — Schema completo, Firebase backend
Este ficheiro (V11.1_FIREBASE_STORAGE_TASKS.md)

Workflow obrigatório POR TASK:

Lê a task completa
Implementa as alterações
TESTA conforme critérios da task
SÓ se testes passarem → Git commit com mensagem da task
Passa à próxima task

Commits: Usa formato feat(v11.1): [nome da task]
Se algum teste FALHAR: PARA, reporta o erro, NÃO avances para task seguinte.

✅ PRÉ-REQUISITOS
Antes de iniciar Task 1, valida:

 Branch V3.2-firebase-storage criada e checkout
 Firebase Console aberto (Storage Rules já deployadas manualmente)
 Ficheiros existem: firebase-config.js, firebase-data.js, Index_v11.0.html, lobby.html


🎯 TASK 1: Adicionar Storage SDK ao firebase-config.js
Contexto
O Firebase Storage SDK ainda não está importado. Precisamos adicionar o import e inicializar o serviço.
Ficheiro
firebase-config.js
Alterações
LOCALIZAR (aproximadamente linhas 1-5):
javascriptimport 'https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js';
import 'https://www.gstatic.com/firebasejs/10.7.1/firebase-auth-compat.js';
import 'https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js';
ADICIONAR APÓS a última import:
javascriptimport 'https://www.gstatic.com/firebasejs/10.7.1/firebase-storage-compat.js';
LOCALIZAR (aproximadamente linha 15, após firebase.initializeApp(firebaseConfig);):
javascriptconst db = firebase.firestore();
ADICIONAR APÓS:
javascriptconst storage = firebase.storage();
Testes
Teste 1.1 - Syntax:
bash# Abre qualquer HTML que usa firebase-config.js (ex: login.html)
# Verifica browser console: 0 erros
Teste 1.2 - Storage disponível:
javascript// Browser console
firebase.storage()
// Esperado: Object {...} (não undefined)
Critérios de Sucesso

 Ficheiro sem erros sintaxe
 firebase.storage() retorna objecto (não undefined)
 Console browser: 0 erros ao carregar página

Commit
bashgit add firebase-config.js
git commit -m "feat(v11.1): adiciona Storage SDK ao firebase-config"

🎯 TASK 2: Criar Generic Storage Layer em firebase-data.js
Contexto
Criar funções genéricas (type-agnostic) para upload/download/delete de assets. Implementação específica para floor images (v11.1), mas preparada para PDFs/DXF/etc (futuro).
Ficheiro
firebase-data.js
Alterações
ADICIONAR ao final do ficheiro (antes do último } se houver, ou no final absoluto):
javascript// ============================================================
// FIREBASE STORAGE LAYER (v11.1) - GENERIC ASSET MANAGEMENT
// ============================================================

/**
 * Upload generic asset to Firebase Storage
 * @param {string} projectId - Project UUID
 * @param {string} path - Relative path under projects/{id}/ (e.g., "floors/123/image.png")
 * @param {File|Blob} file - Binary data
 * @param {Object} metadata - Optional { contentType, customMetadata }
 * @returns {Promise<Object>} { storageURL, downloadURL, expiry }
 */
async function uploadAsset(projectId, path, file, metadata = {}) {
  const fullPath = `projects/${projectId}/${path}`;
  const ref = storage.ref(fullPath);
  
  await ref.put(file, metadata);
  const downloadURL = await ref.getDownloadURL();
  const expiry = Date.now() + (7 * 24 * 60 * 60 * 1000); // 7 days TTL
  
  return {
    storageURL: `gs://${storage.app.options.storageBucket}/${fullPath}`,
    downloadURL,
    expiry
  };
}

/**
 * Get asset download URL with cache TTL validation
 * @param {string} storageURL - gs:// path
 * @param {string} cachedURL - Previously cached download URL
 * @param {number} expiry - Cached URL expiry timestamp
 * @returns {Promise<string>} Valid download URL
 */
async function getAssetURL(storageURL, cachedURL, expiry) {
  const now = Date.now();
  
  if (cachedURL && expiry > now) {
    return cachedURL;
  }
  
  const ref = storage.refFromURL(storageURL);
  return await ref.getDownloadURL();
}

/**
 * Delete asset from Storage
 * @param {string} storageURL - gs:// path
 * @returns {Promise<void>}
 */
async function deleteAsset(storageURL) {
  if (!storageURL) return;
  const ref = storage.refFromURL(storageURL);
  await ref.delete();
}

/**
 * Upload floor image (PNG/JPG)
 * @param {string} projectId - Project UUID
 * @param {number} floorId - Floor timestamp ID
 * @param {File} file - Image file
 * @returns {Promise<Object>} { imageURL, imageDownloadURL, imageURLExpiry }
 */
async function uploadFloorImage(projectId, floorId, file) {
  const path = `floors/${floorId}/image.png`;
  
  const result = await uploadAsset(projectId, path, file, {
    contentType: file.type,
    customMetadata: {
      floorId: floorId.toString(),
      uploadedBy: firebase.auth().currentUser?.email || 'unknown'
    }
  });
  
  return {
    imageURL: result.storageURL,
    imageDownloadURL: result.downloadURL,
    imageURLExpiry: result.expiry
  };
}

/**
 * Get floor image download URL (with cache refresh if expired)
 * @param {Object} floor - Floor object from projectData
 * @returns {Promise<string|null>} Valid download URL or null
 */
async function getFloorImageURL(floor) {
  if (!floor.imageURL) return null;
  
  const url = await getAssetURL(
    floor.imageURL,
    floor.imageDownloadURL,
    floor.imageURLExpiry
  );
  
  // If regenerated → update Firestore cache
  if (url !== floor.imageDownloadURL) {
    const projectId = new URLSearchParams(location.search).get('project');
    const floorIndex = projectData.floors.findIndex(f => f.id === floor.id);
    
    if (floorIndex !== -1) {
      await db.collection('projects').doc(projectId).update({
        [`floors.${floorIndex}.imageDownloadURL`]: url,
        [`floors.${floorIndex}.imageURLExpiry`]: Date.now() + (7 * 24 * 60 * 60 * 1000)
      });
      
      floor.imageDownloadURL = url;
      floor.imageURLExpiry = Date.now() + (7 * 24 * 60 * 60 * 1000);
    }
  }
  
  return url;
}

/**
 * Delete floor image from Storage
 * @param {string} imageURL - gs:// storage path
 * @returns {Promise<void>}
 */
async function deleteFloorImage(imageURL) {
  await deleteAsset(imageURL);
}
Testes
Teste 2.1 - Syntax:
bash# Abre Index_v11.0.html no browser
# Console: 0 erros sintaxe JavaScript
Teste 2.2 - Funções disponíveis:
javascript// Browser console (em Index_v11.0.html autenticado)
typeof uploadFloorImage
// Esperado: "function"

typeof getFloorImageURL
// Esperado: "function"

typeof deleteFloorImage
// Esperado: "function"
Critérios de Sucesso

 Ficheiro sem erros sintaxe
 6 funções definidas (uploadAsset, getAssetURL, deleteAsset, uploadFloorImage, getFloorImageURL, deleteFloorImage)
 Console browser: 0 erros

Commit
bashgit add firebase-data.js
git commit -m "feat(v11.1): cria generic Storage Layer com floor image wrappers"

🎯 TASK 3: Criar Index_v11.1.html com Lazy Loading
Contexto
Duplicar Index_v11.0.html e adicionar: (1) método loadImage async na classe FloorViewer, (2) modificar initFloorViewer() para lazy load, (3) modificar deleteFloor() para apagar Storage blob.
Ficheiro
Duplicar Index_v11.0.html → Index_v11.1.html
Alterações
Alteração 3.1 - Título e Versão
LOCALIZAR (aproximadamente linha 8):
html<title>SSOT JSJ Template · v11.0 (Firebase Backend)</title>
SUBSTITUIR por:
html<title>SSOT JSJ Template · v11.1 (Firebase Storage)</title>
LOCALIZAR (aproximadamente linha 2080):
javascriptconst version = 'v11.0';
SUBSTITUIR por:
javascriptconst version = 'v11.1';

Alteração 3.2 - Adicionar método loadImage à classe FloorViewer
LOCALIZAR a classe FloorViewer (aproximadamente linha 3250) e o método render() dentro dela.
ADICIONAR ANTES do método render():
javascript  /**
   * Lazy load floor image (v11.1)
   * @param {Object} floor - Floor object
   * @returns {Promise<string>} Object URL for canvas rendering
   */
  async loadImage(floor) {
    if (floor.imageLoaded && floor.imageData) {
      return floor.imageData;
    }
    
    const downloadURL = await getFloorImageURL(floor);
    if (!downloadURL) return null;
    
    const response = await fetch(downloadURL);
    const blob = await response.blob();
    
    floor.imageData = URL.createObjectURL(blob);
    floor.imageLoaded = true;
    
    return floor.imageData;
  }

  /**
   * Cleanup object URLs on destroy
   */
  destroy() {
    if (this.data.imageData) {
      URL.revokeObjectURL(this.data.imageData);
    }
  }

Alteração 3.3 - Modificar initFloorViewer() para lazy load
LOCALIZAR a função initFloorViewer() (aproximadamente linha 3850).
SUBSTITUIR a função completa por:
javascriptasync function initFloorViewer() {
  const selector = document.getElementById('zonamentoFloorSelector');
  if (!selector || !selector.value) return;
  
  const floorId = parseInt(selector.value);
  const floor = projectData.floors.find(f => f.id === floorId);
  if (!floor) return;
  
  // Cleanup previous viewer
  if (window.currentFloorViewer) {
    window.currentFloorViewer.destroy();
  }
  
  window.currentFloorViewer = new FloorViewer('zonamentoCanvas', floor);
  
  // 🆕 v11.1: Lazy load image
  if (floor.imageURL) {
    try {
      await window.currentFloorViewer.loadImage(floor);
      window.currentFloorViewer.render();
    } catch (err) {
      console.error('Failed to load floor image:', err);
      alert('Erro ao carregar imagem do piso');
    }
  }
}

Alteração 3.4 - Modificar deleteFloor() para apagar Storage blob
LOCALIZAR a função deleteFloor(floorId) (aproximadamente linha 2450).
SUBSTITUIR a função completa por:
javascriptasync function deleteFloor(floorId) {
  const floor = projectData.floors.find(f => f.id === floorId);
  if (!floor) return;
  
  if (floor.zones && floor.zones.length > 0) {
    if (!confirm(`Piso "${floor.name}" tem ${floor.zones.length} zona(s). Apagar?`)) {
      return;
    }
  }
  
  // 🆕 v11.1: Delete Storage blob BEFORE Firestore
  if (floor.imageURL) {
    try {
      await deleteFloorImage(floor.imageURL);
    } catch (err) {
      console.warn('Failed to delete floor image from Storage:', err);
    }
  }
  
  projectData.floors = projectData.floors.filter(f => f.id !== floorId);
  renderFloors();
  updateKPIs();
  updateActionsFloorTabs();
  
  await saveCurrentProject();
}
Testes
Teste 3.1 - Ficheiro criado:
bashls -la Index_v11.1.html
# Esperado: ficheiro existe
Teste 3.2 - Título correcto:
bashgrep "v11.1" Index_v11.1.html
# Esperado: encontra "v11.1 (Firebase Storage)" e version = 'v11.1'
Teste 3.3 - Métodos existem:
bashgrep "async loadImage" Index_v11.1.html
# Esperado: encontra método

grep "destroy()" Index_v11.1.html
# Esperado: encontra método
Teste 3.4 - deleteFloor modificado:
bashgrep "deleteFloorImage" Index_v11.1.html
# Esperado: encontra chamada na função deleteFloor
Critérios de Sucesso

 Ficheiro Index_v11.1.html existe
 Título = "v11.1 (Firebase Storage)"
 Variável version = 'v11.1'
 Classe FloorViewer tem métodos loadImage() e destroy()
 Função initFloorViewer() é async e chama loadImage()
 Função deleteFloor() é async e chama deleteFloorImage()

Commit
bashgit add Index_v11.1.html
git commit -m "feat(v11.1): cria Index v11.1 com lazy loading e Storage cleanup"

🎯 TASK 4: Modificar lobby.html para Remover Eager Loading
Contexto
O lobby actual pode tentar carregar imagens Base64. Precisamos garantir que só carrega metadata (sem download de blobs).
Ficheiro
lobby.html
Alterações
LOCALIZAR a função loadProjects() (aproximadamente linhas 150-250).
SUBSTITUIR a função completa por:
javascriptasync function loadProjects() {
  const user = firebase.auth().currentUser;
  if (!user) return;
  
  try {
    const projects = await loadAllProjectsFromFirestore();
    const grid = document.getElementById('projectsGrid');
    
    if (!projects || projects.length === 0) {
      grid.innerHTML = '<p style="text-align:center; color:#666;">Sem projetos. Crie o primeiro!</p>';
      return;
    }
    
    grid.innerHTML = projects.map(p => `
      <div class="project-card" data-id="${p.id}">
        <h3>${p.nome_projeto || 'Sem nome'}</h3>
        <p><strong>ID JSJ:</strong> ${p.id_jsj || '---'}</p>
        <p><strong>Pisos:</strong> ${p.floors?.length || 0}</p>
        <p><strong>Fase:</strong> ${p.fase_atual || '---'}</p>
        <div class="card-actions">
          <button onclick="openProject('${p.id}')" class="btn-primary">Abrir</button>
          <button onclick="deleteProjectConfirm('${p.id}')" class="btn-danger">Apagar</button>
        </div>
      </div>
    `).join('');
    
  } catch (err) {
    console.error('Erro ao carregar projetos:', err);
    alert('Erro ao carregar projetos: ' + err.message);
  }
}
VERIFICAR: Não deve existir código que:

Acede floor.imageData
Faz fetch() de imagens
Cria <img> tags com Base64
Chama funções de imagem

Testes
Teste 4.1 - Lobby carrega rápido:
bash# 1. Abre lobby.html no browser (autenticado)
# 2. Abre DevTools → Network tab
# 3. Recarrega página
# 4. Verifica: NÃO há downloads de blobs Storage (*.png, firebasestorage.googleapis.com)
# 5. Tempo total load < 1s (com 5-10 projetos)
Teste 4.2 - Cards mostram metadata:
bash# Valida visualmente:
# - Nome projeto aparece
# - ID JSJ aparece
# - Número de pisos aparece
# - Fase aparece
# - Botões "Abrir" e "Apagar" funcionam
Critérios de Sucesso

 Lobby carrega <1s (Network tab)
 Zero downloads de imagens Storage
 Cards mostram metadata correcta
 Botões "Abrir" e "Apagar" funcionam

Commit
bashgit add lobby.html
git commit -m "feat(v11.1): remove eager image loading do lobby (metadata only)"

🎯 TASK 5: Criar Migration Script
Contexto
Script standalone para converter projectos v11.0 (Base64 imageData) → v11.1 (Storage URLs). Execução manual única.
Ficheiro
Criar novo migrate-v11.0-to-v11.1.html
Conteúdo
Criar ficheiro completo com este conteúdo:
html<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Migration v11.0 → v11.1 (Base64 → Storage)</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { 
      font-family: 'Courier New', monospace; 
      padding: 20px; 
      background: #0a0a0a; 
      color: #00ff00; 
      line-height: 1.6;
    }
    h1 { 
      color: #00ff00; 
      margin-bottom: 20px; 
      font-size: 24px;
      border-bottom: 2px solid #00ff00;
      padding-bottom: 10px;
    }
    .warning { 
      background: #331a00; 
      border: 2px solid #ff6600; 
      padding: 15px; 
      margin: 20px 0; 
      color: #ff6600;
      font-weight: bold;
    }
    button { 
      padding: 12px 24px; 
      font-size: 16px; 
      margin: 10px 5px; 
      cursor: pointer;
      background: #003300;
      color: #00ff00;
      border: 2px solid #00ff00;
      font-family: inherit;
      transition: all 0.3s;
    }
    button:hover { 
      background: #00ff00; 
      color: #000; 
    }
    button:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }
    #log { 
      white-space: pre-wrap; 
      border: 2px solid #00ff00; 
      padding: 15px; 
      margin-top: 20px; 
      background: #000;
      max-height: 500px;
      overflow-y: auto;
      font-size: 14px;
    }
    .error { color: #ff0000; font-weight: bold; }
    .success { color: #00ff00; }
    .warning-log { color: #ffff00; }
    .info { color: #00aaff; }
    #progress {
      margin: 20px 0;
      padding: 10px;
      background: #001a00;
      border: 1px solid #00ff00;
      display: none;
    }
    #progress.active { display: block; }
  </style>
</head>
<body>
  <h1>🔄 MIGRATION v11.0 → v11.1</h1>
  
  <div class="warning">
    ⚠️ <strong>ATENÇÃO - LEIA ANTES DE EXECUTAR</strong><br>
    1. Este script DEVE ser executado APENAS UMA VEZ<br>
    2. Faz BACKUP do Firestore antes (Firebase Console → Firestore → Export)<br>
    3. Valida que tens projetos v11.0 com campo "imageData" (Base64)<br>
    4. Execução NÃO é reversível automaticamente<br>
    5. Fecha todas as outras tabs do SSOT durante migração
  </div>

  <div id="progress">
    <strong>Progresso:</strong> <span id="progressText">Aguardando...</span>
  </div>
  
  <button id="startBtn" onclick="startMigration()">▶️ Iniciar Migração</button>
  <button onclick="clearLog()">🗑️ Limpar Log</button>
  <button id="downloadBtn" onclick="downloadLog()" style="display:none;">💾 Download Log</button>
  
  <div id="log"></div>

  <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-storage-compat.js"></script>
  <script src="firebase-config.js"></script>

  <script>
    const db = firebase.firestore();
    const storage = firebase.storage();
    let migrationLog = [];
    
    function log(msg, type = 'info') {
      const timestamp = new Date().toISOString();
      const logEntry = `${timestamp} | ${msg}`;
      migrationLog.push(logEntry);
      
      const logDiv = document.getElementById('log');
      const className = type === 'error' ? 'error' : 
                       type === 'success' ? 'success' : 
                       type === 'warning' ? 'warning-log' : 
                       'info';
      logDiv.innerHTML += `<div class="${className}">${logEntry}</div>`;
      logDiv.scrollTop = logDiv.scrollHeight;
    }
    
    function clearLog() {
      document.getElementById('log').innerHTML = '';
      migrationLog = [];
      document.getElementById('downloadBtn').style.display = 'none';
    }
    
    function downloadLog() {
      const blob = new Blob([migrationLog.join('\n')], { type: 'text/plain' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `migration-log-${Date.now()}.txt`;
      a.click();
      URL.revokeObjectURL(url);
    }
    
    function updateProgress(text) {
      const progress = document.getElementById('progress');
      const progressText = document.getElementById('progressText');
      progress.classList.add('active');
      progressText.textContent = text;
    }
    
    function base64ToBlob(base64, contentType = '') {
      const byteCharacters = atob(base64);
      const byteArrays = [];
      
      for (let offset = 0; offset < byteCharacters.length; offset += 512) {
        const slice = byteCharacters.slice(offset, offset + 512);
        const byteNumbers = new Array(slice.length);
        
        for (let i = 0; i < slice.length; i++) {
          byteNumbers[i] = slice.charCodeAt(i);
        }
        
        byteArrays.push(new Uint8Array(byteNumbers));
      }
      
      return new Blob(byteArrays, { type: contentType });
    }
    
    async function uploadFloorImage(projectId, floorId, blob) {
      const path = `projects/${projectId}/floors/${floorId}/image.png`;
      const ref = storage.ref(path);
      
      await ref.put(blob, {
        contentType: 'image/png',
        customMetadata: {
          floorId: floorId.toString(),
          migratedFrom: 'v11.0',
          migratedAt: new Date().toISOString()
        }
      });
      
      const downloadURL = await ref.getDownloadURL();
      const expiry = Date.now() + (7 * 24 * 60 * 60 * 1000); // 7 days
      
      return {
        imageURL: `gs://${storage.app.options.storageBucket}/${path}`,
        imageDownloadURL: downloadURL,
        imageURLExpiry: expiry
      };
    }
    
    async function migrateProject(projectId) {
      log(`\n📂 Processando projeto ${projectId}...`, 'info');
      updateProgress(`Migrando projeto ${projectId}...`);
      
      const docRef = db.collection('projects').doc(projectId);
      const doc = await docRef.get();
      
      if (!doc.exists) {
        log(`❌ Projeto ${projectId} não encontrado`, 'error');
        return { success: false, migrated: 0, skipped: 0 };
      }
      
      const data = doc.data();
      let migratedCount = 0;
      let skippedCount = 0;
      
      if (!data.floors || data.floors.length === 0) {
        log(`  ⚠️  Projeto sem pisos (skip)`, 'warning');
        return { success: true, migrated: 0, skipped: 0 };
      }
      
      for (let i = 0; i < data.floors.length; i++) {
        const floor = data.floors[i];
        
        // Já migrado?
        if (floor.imageURL) {
          log(`  ✅ Floor ${floor.id} (${floor.name || 'sem nome'}) já migrado`, 'success');
          skippedCount++;
          continue;
        }
        
        // Sem imagem?
        if (!floor.imageData) {
          log(`  ⚠️  Floor ${floor.id} (${floor.name || 'sem nome'}) sem imageData (skip)`, 'warning');
          skippedCount++;
          continue;
        }
        
        try {
          log(`  🔄 Migrando floor ${floor.id} (${floor.name || 'sem nome'})...`, 'info');
          updateProgress(`Projeto ${projectId}: Floor ${i+1}/${data.floors.length}`);
          
          // Extract Base64
          const base64Data = floor.imageData.includes(',') 
            ? floor.imageData.split(',')[1] 
            : floor.imageData;
          
          const blob = base64ToBlob(base64Data, 'image/png');
          
          // Upload to Storage
          const result = await uploadFloorImage(projectId, floor.id, blob);
          
          // Update Firestore
          await docRef.update({
            [`floors.${i}.imageURL`]: result.imageURL,
            [`floors.${i}.imageDownloadURL`]: result.imageDownloadURL,
            [`floors.${i}.imageURLExpiry`]: result.imageURLExpiry,
            [`floors.${i}.imageData`]: firebase.firestore.FieldValue.delete()
          });
          
          log(`  ✅ Floor ${floor.id} migrado com sucesso (${(blob.size/1024).toFixed(1)} KB)`, 'success');
          migratedCount++;
          
        } catch (err) {
          log(`  ❌ ERRO ao migrar floor ${floor.id}: ${err.message}`, 'error');
          console.error('Migration error details:', err);
          throw err; // Stop migration on error
        }
      }
      
      log(`🎉 Projeto ${projectId} completo: ${migratedCount} migrados, ${skippedCount} skipped\n`, 'success');
      return { success: true, migrated: migratedCount, skipped: skippedCount };
    }
    
    async function startMigration() {
      const user = firebase.auth().currentUser;
      if (!user) {
        alert('❌ Não autenticado. Faça login em login.html primeiro.');
        return;
      }
      
      const confirmation = confirm(
        '⚠️ CONFIRMAR MIGRAÇÃO?\n\n' +
        '✓ Backup do Firestore feito?\n' +
        '✓ Primeira execução deste script?\n' +
        '✓ Todas as outras tabs SSOT fechadas?\n\n' +
        'CLIQUE OK para continuar.\n' +
        'CLIQUE CANCELAR para abortar.'
      );
      
      if (!confirmation) {
        log('❌ Migração cancelada pelo utilizador', 'warning');
        return;
      }
      
      // Disable button
      const startBtn = document.getElementById('startBtn');
      startBtn.disabled = true;
      startBtn.textContent = '⏳ A migrar...';
      
      clearLog();
      log(`🚀 Iniciando migração para user ${user.email}...`, 'info');
      log(`⏰ Timestamp início: ${new Date().toISOString()}\n`, 'info');
      
      let totalMigrated = 0;
      let totalSkipped = 0;
      let totalProjects = 0;
      let failedProjects = 0;
      
      try {
        updateProgress('A carregar projetos...');
        
        const snapshot = await db.collection('projects')
          .where('owner', '==', user.uid)
          .get();
        
        totalProjects = snapshot.size;
        log(`📊 Encontrados ${totalProjects} projetos\n`, 'info');
        
        if (totalProjects === 0) {
          log('⚠️  Sem projetos para migrar', 'warning');
          return;
        }
        
        for (const doc of snapshot.docs) {
          try {
            const result = await migrateProject(doc.id);
            totalMigrated += result.migrated;
            totalSkipped += result.skipped;
          } catch (err) {
            failedProjects++;
            log(`💥 ERRO FATAL no projeto ${doc.id}: ${err.message}`, 'error');
            
            const continueOnError = confirm(
              `❌ Erro no projeto ${doc.id}:\n${err.message}\n\n` +
              'Continuar com os restantes projetos?'
            );
            
            if (!continueOnError) {
              throw new Error('Migração abortada pelo utilizador após erro');
            }
          }
        }
        
        // Summary
        log('\n========================================', 'success');
        log('🎉🎉🎉 MIGRAÇÃO COMPLETA!', 'success');
        log('========================================', 'success');
        log(`📊 Projetos processados: ${totalProjects}`, 'info');
        log(`✅ Floors migrados: ${totalMigrated}`, 'success');
        log(`⚠️  Floors skipped: ${totalSkipped}`, 'warning');
        log(`❌ Projetos com erro: ${failedProjects}`, failedProjects > 0 ? 'error' : 'info');
        log(`⏰ Timestamp fim: ${new Date().toISOString()}`, 'info');
        log('========================================\n', 'success');
        
        if (failedProjects > 0) {
          log('⚠️  ATENÇÃO: Alguns projetos falharam. Revê o log acima.', 'warning');
        }
        
        log('📝 Próximos passos:', 'info');
        log('1. Download do log (botão acima)', 'info');
        log('2. Valida Firestore Console: doc sizes < 100KB', 'info');
        log('3. Valida Storage Console: blobs criados', 'info');
        log('4. Testa Index_v11.1.html com projetos migrados', 'info');
        
        updateProgress('Concluído!');
        document.getElementById('downloadBtn').style.display = 'inline-block';
        
      } catch (err) {
        log(`\n💥 ERRO FATAL GERAL: ${err.message}`, 'error');
        console.error('Fatal migration error:', err);
        updateProgress('ERRO - Migração interrompida');
      } finally {
        startBtn.disabled = false;
        startBtn.textContent = '▶️ Iniciar Migração';
      }
    }
    
    // Auth check
    firebase.auth().onAuthStateChanged(user => {
      if (!user) {
        log('⚠️  Não autenticado. Redirecionando para login...', 'warning');
        setTimeout(() => window.location.href = 'login.html', 3000);
      } else {
        log(`✅ Autenticado como ${user.email}`, 'success');
        log(`🔒 Pronto para migrar projetos do owner: ${user.uid}\n`, 'info');
      }
    });
  </script>
</body>
</html>
Testes
Teste 5.1 - Ficheiro criado:
bashls -la migrate-v11.0-to-v11.1.html
# Esperado: ficheiro existe
Teste 5.2 - Syntax HTML válida:
bash# Abre ficheiro no browser
# Console: 0 erros JavaScript
# UI aparece com título "MIGRATION v11.0 → v11.1"
Teste 5.3 - Auth funciona:
bash# Abre migrate-v11.0-to-v11.1.html no browser
# Se não autenticado → redirige para login.html após 3s
# Se autenticado → mostra "Autenticado como [email]"
Critérios de Sucesso

 Ficheiro migrate-v11.0-to-v11.1.html existe
 UI renderiza sem erros console
 Auth guard funciona (redirige se não autenticado)
 Botão "Iniciar Migração" visível

IMPORTANTE: NÃO executar o script ainda. Apenas validar que está criado e sem erros.
Commit
bashgit add migrate-v11.0-to-v11.1.html
git commit -m "feat(v11.1): cria migration script v11.0 to v11.1"

🎯 TASK 6: Teste End-to-End (Desenvolvimento)
Contexto
Validar que todo o fluxo funciona ANTES de executar migration em produção.
Procedimento
Teste 6.1 - Criar projeto teste:

Abre Index_v11.1.html no browser (autenticado)
Cria novo projeto: "TESTE V11.1"
Adiciona 1 piso: "Piso 0"
NÃO fazes upload de imagem ainda
Valida: Projeto salva em Firestore (lobby mostra)

Teste 6.2 - Upload imagem (simulação Storage):

No projeto "TESTE V11.1", edita Piso 0
PROBLEMA: Index_v11.1.html ainda não tem UI para upload!
SOLUÇÃO TEMPORÁRIA: Vamos criar função teste no console

Adiciona ao browser console (em Index_v11.1.html):
javascriptasync function testUploadImage() {
  // Cria um blob de teste (quadrado vermelho 100x100px)
  const canvas = document.createElement('canvas');
  canvas.width = 100;
  canvas.height = 100;
  const ctx = canvas.getContext('2d');
  ctx.fillStyle = 'red';
  ctx.fillRect(0, 0, 100, 100);
  
  const blob = await new Promise(resolve => canvas.toBlob(resolve, 'image/png'));
  
  const projectId = new URLSearchParams(location.search).get('project');
  const floor = projectData.floors[0]; // Primeiro piso
  
  console.log('Uploading test image...');
  const result = await uploadFloorImage(projectId, floor.id, blob);
  
  console.log('Upload result:', result);
  
  // Update local state
  floor.imageURL = result.imageURL;
  floor.imageDownloadURL = result.imageDownloadURL;
  floor.imageURLExpiry = result.imageURLExpiry;
  
  // Save to Firestore
  await saveCurrentProject();
  
  console.log('✅ Test image uploaded and saved!');
}

testUploadImage();
Teste 6.3 - Validar Storage:

Firebase Console → Storage → projects/[id]/floors/[id]/image.png existe?
Firestore Console → projects/[id] → floors[0] tem imageURL, imageDownloadURL, imageURLExpiry?

Teste 6.4 - Lazy load no Viewer:

Secção 7 → Zonamento → Seleciona "Piso 0"
Network tab: Download da imagem acontece AGORA (não antes)
Canvas mostra quadrado vermelho?
Muda para outro piso e volta: Network tab NÃO download (cached)

Teste 6.5 - Delete blob:

Secção 2 → Apaga "Piso 0"
Confirma apagar
Firebase Console → Storage: blob apagado?
Firestore Console → projects/[id] → floors array vazio?

Critérios de Sucesso

 Upload imagem cria blob Storage
 Firestore tem URLs (não Base64)
 Lazy load funciona (Network tab valida)
 Cache funciona (switching pisos instant)
 Delete piso apaga blob Storage
 Console: 0 erros em todo o fluxo

Commit
bash# Sem commit - é teste apenas
# Se TODOS os testes passarem, continua para TASK 7
# Se ALGUM teste FALHAR, PARA e reporta erro

🎯 TASK 7: Executar Migration (CRÍTICO)
⚠️ PRÉ-CONDIÇÕES OBRIGATÓRIAS
ANTES de executar, VALIDA:

 TASK 6 passou 100% (todos testes OK)
 Backup Firestore exportado (Firebase Console → Firestore → Export)
 Ficheiro backup local guardado (nome: firestore-backup-[data].json)
 Projetos v11.0 existem com imageData Base64
 Todas as outras tabs SSOT fechadas
 Hora marcada (sem interrupções durante ~10-30min)

SE ALGUMA CONDIÇÃO FALHAR → NÃO EXECUTAR
Procedimento
Passo 7.1 - Validação pré-migration:
javascript// Browser console em Index_v11.0.html
const project = await db.collection('projects').limit(1).get();
const data = project.docs[0].data();
console.log('Sample floor:', data.floors[0]);

// Esperado: floor tem campo "imageData" (Base64 string)
// Se já tem "imageURL" → Projeto já migrado (skip migration)
Passo 7.2 - Executar migration:

Abre migrate-v11.0-to-v11.1.html no browser
Valida autenticação (mostra email)
Lê o warning vermelho completo
Clica "▶️ Iniciar Migração"
Confirma popup (OK)
AGUARDA até log mostrar "MIGRAÇÃO COMPLETA"
NÃO FECHES A TAB até download do log

Passo 7.3 - Download log:

Clica "💾 Download Log"
Guarda ficheiro: migration-log-[timestamp].txt

Passo 7.4 - Validação pós-migration:
Firebase Console → Firestore:
bash# Abre projeto migrado
# Valida: floors[0] TEM imageURL, imageDownloadURL, imageURLExpiry
# Valida: floors[0] NÃO TEM imageData
# Valida: Document size < 100KB (era ~500KB-2MB antes)
Firebase Console → Storage:
bash# Abre projects/[id]/floors/[id]/
# Valida: image.png existe
# Valida: Metadata tem customMetadata.migratedFrom = "v11.0"
Passo 7.5 - Teste funcional pós-migration:

Abre Index_v11.1.html
Abre projeto migrado
Secção 7 → Zonamento → Seleciona piso migrado
Valida: Imagem carrega e aparece no canvas
Network tab: Download URL foi usado (não Base64)

Critérios de Sucesso

 Migration log mostra "MIGRAÇÃO COMPLETA" sem erros fatais
 Firestore: floors têm imageURL (não imageData)
 Firestore: Document sizes < 100KB
 Storage: Blobs criados em projects/[id]/floors/[id]/image.png
 Index_v11.1.html carrega imagens migradas correctamente
 Console: 0 erros ao carregar imagens

SE ALGUM CRITÉRIO FALHAR:

NÃO CONTINUAR
Reportar erro
Considerar rollback (restore Firestore backup)

Commit
bash# Após migration 100% sucesso
git add .
git commit -m "feat(v11.1): migration v11.0 to v11.1 executada com sucesso"

🎯 TASK 8: Atualizar Documentação
Contexto
Documentar alterações v11.1 nos ficheiros de projeto.
Ficheiros a Modificar
Ficheiro 8.1 - ROADMAP.md
LOCALIZAR (aproximadamente linha 60):
markdown## 🔨 FASE 3.1: FIREBASE STORAGE LAYER (1 semana)

### v11.1 - Generic Asset Storage ⚠️ EM DESENVOLVIMENTO
SUBSTITUIR por:
markdown## ✅ FASE 3.1: FIREBASE STORAGE LAYER (Completa)

### v11.1 - Generic Asset Storage ✅ COMPLETA
**Conclusão**: [DATA ACTUAL]

Ficheiro 8.2 - ESPECIFICACAO.md
LOCALIZAR (aproximadamente linha 1):
markdown# SSOT JSJ Template - Especificação Técnica v11.1

**Versão:** 11.1
**Data:** 15 Fevereiro 2026
SUBSTITUIR Data por data actual.
LOCALIZAR (aproximadamente linha 20, secção Changelog):
markdown### D. Changelog v11.0 → v11.1 🆕
VALIDAR que secção está completa e actualizada.

Ficheiro 8.3 - RISCOS.md
LOCALIZAR (aproximadamente linha 100):
markdown## 10. RISCOS FIREBASE STORAGE v11.1 🆕
VALIDAR que secção existe e está completa (já devia estar na spec).
Se NÃO EXISTIR, adicionar:
markdown## 10. RISCOS FIREBASE STORAGE v11.1

### 10.1 Storage Path Pattern
Path: `projects/{projectId}/floors/{floorId}/image.png`
**CRÍTICO**: Se mudares padrão → Migration script quebra.

### 10.2 URL Expiration (Cache TTL)
- TTL fixo 7 dias
- `getFloorImageURL()` regenera se expirado
- **NÃO alterar TTL** sem validar impacto

### 10.3 Lazy Loading Dependency
**NUNCA** eager load todas as imagens.
**SEMPRE** lazy load on-demand.

### 10.4 Security Rules Sync
Storage Rules validam ownership via Firestore.
Se mudares Firestore schema → **actualiza Storage Rules**.

### 10.5 Migration Script
**SEMPRE** backup Firestore antes.
**NUNCA** executar 2x (duplica blobs).

### 10.6 Delete Order
**SEMPRE** `deleteFloorImage()` ANTES de remover doc Firestore.
Ordem inversa → orphan blobs (leak).

Ficheiro 8.4 - TESTES.md
LOCALIZAR (aproximadamente linha 200):
markdown### 1.9 Teste Storage Upload (v11.1) 🆕
VALIDAR que testes 1.9 a 1.14 existem (já deviam estar na spec).
Se NÃO EXISTIREM, copiar da spec original fornecida.
Testes
Teste 8.1 - Ficheiros modificados:
bashgit status
# Esperado: modified: ROADMAP.md, ESPECIFICACAO.md, RISCOS.md, TESTES.md
Teste 8.2 - ROADMAP marca v11.1 completa:
bashgrep "v11.1.*COMPLETA" ROADMAP.md
# Esperado: encontra linha
Teste 8.3 - Datas actualizadas:
bashgrep "$(date +%Y)" ESPECIFICACAO.md
# Esperado: encontra ano actual
Critérios de Sucesso

 ROADMAP.md: v11.1 marcada ✅ COMPLETA
 ESPECIFICACAO.md: Data actualizada
 RISCOS.md: Secção §10 existe e completa
 TESTES.md: Testes 1.9-1.14 existem

Commit
bashgit add ROADMAP.md ESPECIFICACAO.md RISCOS.md TESTES.md
git commit -m "docs(v11.1): atualiza docs para v11.1 completa"

🎯 TASK 9: Push e Merge
Contexto
Finalizar branch e preparar para merge.
Procedimento
Passo 9.1 - Review final:
bashgit log --oneline
# Esperado: ~8-9 commits (1 por task)

git diff main..V3.2-firebase-storage --stat
# Esperado: 
# - firebase-config.js (modificado)
# - firebase-data.js (modificado, +~200 linhas)
# - Index_v11.1.html (criado)
# - lobby.html (modificado)
# - migrate-v11.0-to-v11.1.html (criado)
# - ROADMAP.md, ESPECIFICACAO.md, RISCOS.md, TESTES.md (modificados)
Passo 9.2 - Push branch:
bashgit push origin V3.2-firebase-storage
Passo 9.3 - Criar tag:
bashgit tag v11.1.0
git push origin v11.1.0
Passo 9.4 - Merge para main (OPCIONAL - aguardar aprovação David):
bash# NÃO EXECUTAR sem aprovação explícita
git checkout main
git merge V3.2-firebase-storage
git push origin main
Critérios de Sucesso

 Branch pushed para remote
 Tag v11.1.0 criada e pushed
 Commits aparecem no GitHub/GitLab

Commit
Não aplicável (apenas push/tag)

✅ CHECKLIST FINAL
Após TODAS as tasks:
Funcional:

 Upload imagem piso → Storage blob criado
 Viewer carrega imagem lazy (on-demand)
 Switching pisos usa cache (instant)
 Delete piso apaga blob Storage
 Migration converteu todos floors Base64 → Storage

Performance:

 Lobby load <500ms (10 floors)
 Viewer image load <1s (500KB PNG)
 Firestore doc size <100KB (10 floors)

Segurança:

 User A não acede blob User B (403)
 Storage Rules validam ownership via Firestore

Código:

 9 commits (1 por task)
 Branch pushed
 Tag v11.1.0 criada
 Docs actualizadas (4 ficheiros)

Migration:

 Backup Firestore guardado
 Migration log downloaded
 100% floors migrados (0 erros fatais)
 Projetos funcionam em Index_v11.1.html


🚨 SE ALGO FALHAR
DURANTE TASKS 1-6:

PARA imediatamente
Reporta erro no commit message ou log
NÃO continua para task seguinte
Aguarda instrução

DURANTE TASK 7 (Migration):

Clica "Download Log" imediatamente
NÃO CLOSES A TAB
Reporta erro com log anexo
Aguarda decisão de rollback

Rollback Firestore:
bash# Firebase Console → Firestore → Import
# Seleciona backup exportado antes da migration
# ATENÇÃO: Isto SOBRESCREVE todos os dados actuais
Rollback Storage (manual):
bash# Firebase Console → Storage
# Apaga blobs em projects/[id]/floors/[id]/ criados pela migration
# (Identifica por metadata.migratedFrom = "v11.0")

📞 CONTACTO
Em caso de dúvida ou erro:

Reporta no chat com logs completos
Inclui: Task number, erro exacto, browser console screenshot
NÃO assumes solução - aguarda confirmação