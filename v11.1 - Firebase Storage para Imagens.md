v11.1 - Firebase Storage para Imagens
Branch base: V3.1-Arquitetura-Firebase-ready (v11.0)
Objetivo: Eliminar Base64 + Resolver nested arrays
Tempo total: ~45 min
Status: 🚧 Em planeamento

MOTIVAÇÃO
Problemas v11.0:

❌ Docs Firestore >1MB (imagens Base64)
❌ Nested arrays (shapes: [[{x,y}]]) crasham saves
❌ Performance: Base64 em cada sync

Solução v11.1:

✅ Storage CDN Google para imagens
✅ Firestore só URLs (<100KB/doc)
✅ Flatten arrays (shapes: [{points: [...]}])


TASKS SEQUENCIAIS
[ ] Task 1: Reset Firestore (Manual) ⏱️ 5 min
Ação:

Abrir: https://console.firebase.google.com/project/jsj-ssot-template/firestore
Collection projects → Menu ⋮ → Delete collection
Confirmar

Validação:

 Firestore vazio (sem collections)


[ ] Task 2: Ativar Storage (Manual) ⏱️ 3 min
Ação:

Abrir: https://console.firebase.google.com/project/jsj-ssot-template/storage
"Get Started" → Modo produção
Rules → Substituir por:

javascriptrules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/{allPaths=**} {
      allow read: if request.auth != null 
                  && request.auth.token.email.matches('.*@jsj\\.pt$');
      allow write: if request.auth != null 
                   && request.auth.token.email.matches('.*@jsj\\.pt$');
    }
  }
}

Publish

Validação:

 Storage ativado
 Rules guardadas


[ ] Task 3: Criar firebase-storage.js ⏱️ 15 min
Ficheiro: firebase-storage.js (raiz)
Funções necessárias:
javascript// 1. Upload imagem → URL
async uploadFloorImage(projectId, floorId, imageBlob) → string

// 2. Apagar imagem
async deleteFloorImage(imageUrl) → void

// 3. Converter Base64 → Blob
base64ToBlob(base64String) → Blob
```

**Estrutura Storage**:
```
projects/
  {projectId}/
    floors/
      {floorId}.png
Imports:
javascriptimport { getStorage, ref, uploadBytes, getDownloadURL, deleteObject } 
from 'firebase/storage';
Validação:

 Ficheiro criado
 3 funções exportadas
 Sem erros sintaxe


[ ] Task 4: Modificar firebase-data.js ⏱️ 10 min
Função alvo: saveProjectToFirestore()
Mudanças:
1. Import Storage:
javascriptimport { uploadFloorImage, base64ToBlob } from './firebase-storage.js';
2. Antes de salvar Firestore:
javascript// a) Upload imagens
for (const floor of projectData.floors.values()) {
  if (floor.actionsData?.blueprint?.imageData?.startsWith('data:image')) {
    const blob = base64ToBlob(floor.actionsData.blueprint.imageData);
    const url = await uploadFloorImage(projectData.id, floor.id, blob);
    
    floor.actionsData.blueprint.imageUrl = url;
    delete floor.actionsData.blueprint.imageData;
  }
}

// b) Flatten shapes
const firestoreData = flattenProjectData(projectData);

// c) Save
await db.collection('projects').doc(projectData.id).set(firestoreData);
3. Nova função:
javascriptfunction flattenProjectData(data) {
  // Maps → Arrays
  // shapes: [[{x,y}]] → shapes: [{points: [{x,y}]}]
}
Validação:

 Upload antes de Firestore write
 Shapes flattened
 Async/await correto
 Sem erros console


[ ] Task 5: Modificar Index_v11.0.html ⏱️ 10 min
Função alvo: loadAllData()
Mudanças:
1. Após carregar de Firestore:
javascript// Para cada floor com imageUrl
if (floor.actionsData?.blueprint?.imageUrl) {
  // Fetch → Base64 (compatibilidade zonas.html)
  const base64 = await urlToBase64(floor.actionsData.blueprint.imageUrl);
  floor.actionsData.blueprint.imageData = base64; // Runtime only
}

// De-flatten shapes
// shapes: [{points: [...]}] → shapes: [[...]]
2. Nova função:
javascriptasync function urlToBase64(url) {
  const response = await fetch(url);
  const blob = await response.blob();
  return new Promise((resolve) => {
    const reader = new FileReader();
    reader.onloadend = () => resolve(reader.result);
    reader.readAsDataURL(blob);
  });
}
Validação:

 Imagens carregam do Storage
 zonas.html recebe Base64
 Canvas renderiza OK
 Sem erros console


[ ] Task 6: Testes End-to-End ⏱️ 10 min
Fluxo:

Abrir login.html local
Autenticar @jsj.pt
Criar projeto novo
Adicionar piso + imagem PNG
Abrir "Editor de Zonas"
Desenhar 1 polígono
Enviar dados → Index
Voltar lobby → Reabrir projeto

Validações:

 Imagem aparece no Editor
 Polígono renderiza
 Storage Console tem: projects/{id}/floors/{id}.png
 Firestore doc <100KB (sem Base64)
 Real-time sync funciona
 Console sem erros "nested arrays"


ROLLBACK
Se falhar:
bashgit checkout "Arquitetura Firebase ready"
```

Dados Firestore já apagados (Task 1), mas sem problema (eram corrompidos).

---

## APÓS CONCLUSÃO

**Commit message**:
```
feat(v11.1): Firebase Storage para imagens + flatten arrays

- Upload imagens para Storage (CDN)
- Firestore só URLs (<100KB/doc)
- Resolve nested arrays crash
- Compatibilidade zonas.html mantida

BREAKING: Collection projects resetada (dados corrompidos v11.0)
Atualizar:

 AGENTE_MIGRACAO.md (entrada v11.0 → v11.1)
 AGENTE_ROADMAP.md (marcar Fase 3 completa)